spring:
  application:
    name: mscloudgateway
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id: true
server:
  port:8080

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultzone: http://localhost:8761/eureka